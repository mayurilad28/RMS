{
  "name": "resume-scanner-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root — orchestrates the Angular frontend and the Express serverless API for deployment on Vercel.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend"
  }
}
