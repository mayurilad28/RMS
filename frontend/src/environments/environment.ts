/**
 * Development environment.
 *
 * Used by `ng serve` and any non-production build. Points the frontend
 * at the locally-running Node API on port 5000.
 */
export const environment = {
  production: false,
  apiBase: 'http://localhost:5000',
};
